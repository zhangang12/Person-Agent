# 优化方向·第二波：上下文管理 / Harness 工程 / 自测思维 / Loop 工程

> **日期**：2026-07-27。**定位**：第一波（P0-P3，见 `docs/工作汇总-内网opencode弱模型整改.md`）+ 并行波（验证证据闸/golden 回放/192k 口径/普通卡交棒/基线测量）之后的**剩余增量清单**。每项都先排除了已有机制（AGENTS.md 四道防线 §82 为现状基线），按"解决什么/依据/落点/成本"组织。
> **一句话**：第一波的机制大多"能跑了但没度量"，第二波的主线是把**度量与收口**补上——让每条机制可验证、可运营、可回归。

## 一、上下文管理

| # | 方向 | 解决什么 | 依据/落点 | 成本 |
|---|---|---|---|---|
| C1 | **被动压缩质量探针（forkcheck T7）** | context-guard 的五条压缩纪律**写了但从未真触发验证过**——造一个逼近上限的会话触发 serve 被动压缩，验证纪律真进了压缩提示词、摘要含 todo/已读清单 | scripts/fork-capability-probe.mjs 加 T7 | 半天 |
| C2 | **压缩语义实测对比（D5 遗留）** | 卡级"摘要+新会话" vs serve"原地锚定续用"两条路径并存，弱模型上谁续跑质量高没有数据——各跑 3 个真实任务对比，定主推路径并文档化触发线 | 借鉴总清单 D5；ui-vue compactCore vs serve /summarize | 1 天 |
| C3 | **注入构成看板常态化** | forkcheck T2c 构成估算只是单次快照；记忆/知识/技能只增不减，首发基线逐月垫高——每张卡首发注入构成分项统计+超预算告警，给 A5 知识整固一个量化触发条件 | src/session.js 注入段打点 + 设置页小面板 | 1 天 |
| C4 | **老旧 reasoning 块纳入清理** | context-guard 现在只清 tool 结果；弱模型思考链是第二大占用——保最近 N 轮 reasoning，更老的替换为"[思考过程已折叠]"，同样同 ID 同决策 | plugin/bocomhermes-context-guard.js cleanPart 扩展一类 part | 半天-1 天 |
| C5 | **重复全读去重提醒** | 同一文件被反复全量 read（CC 记忆预取去重思想）：会话内 file→reads 计数，第二次全读即提醒"已在上下文，引用即可" | src/session.js 读字节计量（③b）旁挂计数 | 半天 |
| C6 | **KV 命中率趋势与回归告警** | ctx chip 只有单次命中率；提示词/注入顺序改动是否打挂缓存没有度量——按卡/按日趋势存储，命中率骤降即告警，提示词工程的度量闭环 | opencode.js noteUsage + 审计流水 | 1 天 |

## 二、Harness 工程

| # | 方向 | 解决什么 | 依据/落点 | 成本 |
|---|---|---|---|---|
| H1 | **统一回合末注入队列（时机已到）** | 验证棒/证据闸/普通卡交棒进来后注入源更多，多点各自判忙的回归温床扩大——收口为每会话一个队列：所有注入方只入队，排空点统一收"回合结束"，优先级与 handoffDue 互斥只写一份 | 借鉴清单 ⑥/A4；src/session.js + ui-vue store 轮末链 | 2-3 天（动主路径，配回归） |
| H2 | **审批疲劳 → 规则建议** | 权限规则上线了但靠用户自己想到去配——统计每张卡弹框率/规则命中率，高频弹框命令弹建议"这条本周弹了 12 次，加 allow 吗？"（度量驱动规则沉淀的运营闭环） | src/audit.js 已有流水 + 设置页/卡内提示 | 1 天 |
| H3 | **委派质量度量** | 现在只有硬闸管"贴原文"，没有"委派值不值"的度量：子 Agent 回报格式合规率、重读已读文件率、与主 Agent 重复劳动率——喂给主控拆派策略与提示词迭代 | 借鉴总清单 §9④；session.js 子会话计量 | 1-2 天 |
| H4 | **机判闸卡型扩展** | 绕圈账本目前 wf 主、证据闸目前编码分片——普通卡也会绕圈（反复读同一批文件）；"声明完成但无 diff/无测试记录"的通用机判可推广 | src/session.js 看门狗 + 证据闸 | 1 天 |
| H5 | **Continue vs Spawn 决策规则化** | CC coordinator 的经验：验证类任务用新眼睛、错误方案要 clean slate 防锚定——主控重派分片/验证棒时按这条决策，而不是一律续跑 | 提示词借鉴 §5；window.js 主控重派逻辑 | 半天-1 天 |
| H6 | **弱模型病灶检测器（观测期工具化）** | 两周观测期目前靠人看会话——机检真实 transcript：声称完成但无验证证据/委派贴原文/重读已读文件，自动填台账"待补样本" | docs/项目记忆/弱模型行为台账.md；scripts/ 新检测器 | 1 天 |

## 三、自测思维

> **注意区分**：本节 T1-T6 是**壳层自身**的回归测试基建（防止我们改壳改出回归）。
> 如果你要的是"**Agent 干完活不会自己验证**"（改完前端不看页面、改完后端不调接口就说完成）——那是 harness 验证闭环，见专项方案 `docs/Agent自验闭环-前端后端验证方案.md`（前端浏览器断言三连/后端接口探针/普通卡完成机判/项目验证档案）。

| # | 方向 | 解决什么 | 依据/落点 | 成本 |
|---|---|---|---|---|
| T1 | **提示词请求体回归（golden 注入 diff）** | 提示词改动目前没有效果回归手段，只有观测期人看——同一转录，新旧注入文本生成 serve 请求体，diff 构成与预算（不超线、构成如预期），进 CI 不依赖真模型 | golden 回放层（scripts/replay/）扩展 | 1 天 |
| T2 | **context-guard 长会话 golden** | 清理逻辑现在只测 6 轮合成数据——50 轮真实形状转录喂 messages.transform：第 10 轮前全清、幂等、缓存前缀逐字节稳定 | scripts/context-guard-selftest.mjs 扩例 | 半天 |
| T3 | **`npm run test:all` 统一入口** | 15+ 个自测脚本各自跑，发版回归靠记忆——收口一键，含 forkcheck（需 serve 的标记跳过） | package.json | 1 小时 |
| T4 | **内网验收套件扩展** | 现在 cmd 只有 forkcheck+LSP 冒烟——加 config-patch-probe + ctxguard + readspill，U1 跑一遍全有 | scripts/内网验收.cmd | 半天 |
| T5 | **畸形输入 fuzz** | 弱模型产出本来就畸形——规则解析/转录回放/注入拼装/todo 同步对畸形输入的健壮性用例 | 各 selftest 补 fuzz 组 | 半天-1 天 |
| T6 | **真实转录 golden 库** | 回放层只有手写 case——真实 transcript 抽样脱敏入库做弹药，回归更接近生产 | scripts/replay/cases/ | 持续 |

## 四、Loop 工程（serve 主循环）

| # | 方向 | 解决什么 | 依据/落点 | 成本 |
|---|---|---|---|---|
| L1 | **fork 现状盘点（E 组最大前置）** | E1-E6（主循环/并发分区/边流边执行/错误恢复/CacheSafeParams/KV 断点）移植前必须先知道 fork 与上游差距——opencode 上游已有 CompactionPart/batch_tool/chatMaxRetries/50KB 截断，CC 机制只补缺口防重复移植；产出移植差距清单 | 借鉴总清单 E 组前置；等 U3 邮件回信 + npm run compat | 1 天（主要是盘点文档） |
| L2 | **并行纪律提示词补课** | serve 兜底模板缺"独立工具调用并行发"（anthropic.txt 才有这条）——内网模型落兜底模板，补进 SYS_BLOCK/首发注入，壳层能做的"伪并发分区" | 提示词借鉴 §6；context-guard / session.js | 1 小时（随下波提示词） |
| L3 | **max-steps 防绕圈推广** | agent.explore.maxSteps=30 已配；评估 agent.build.steps 收口——serve 到线注入 max-steps.txt 强制总结，弱模型防失控的原生闸 | opencode.jsonc（P3.1 同通道） | 半天（先实测行为） |
| L4 | **loop 可观测化** | serve 内部的压缩/重试/错误恢复用户看不见——session.error/session.compacted/重试事件在状态行或 chip 可视化（"serve 正在内部压缩/恢复"） | opencode.js 事件路由 + ui-vue 状态行 | 1 天 |
| L5 | **prompt_async 真机验证翻默认** | knob 已上（默认关）——跑一周真实使用，收尾判据无异样后翻默认，退役 R4 在飞断开自愈的整段复杂度 | knobs.promptAsync | 观测 1 周 |
| L6 | **单轮输出长度打点** | 超长回合（弱模型拖沓/JSON 截断前兆）没有度量——回合级输出 token 打点进计量，给 chat.params 收口值定标 | opencode.js noteUsage | 半天 |

## 建议排序

1. **本周可做（便宜且验证已发货的东西）**：C1 压缩探针 T7、T3 test:all、L2 并行纪律（随下波提示词）、T2 长会话 golden
2. **下一个波次（度量主线）**：H6 病灶检测器、C3 注入看板、H2 审批疲劳建议、C6 KV 趋势
3. **结构性（单独排期）**：H1 统一注入队列（动主路径，配足回归）、L1 fork 盘点（等 U3 回信）
4. **观察后定**：C2 压缩语义对比、L5 prompt_async 翻默认、L3 max-steps 推广

**技能层素材（不占上表，来自完备性扫尾）**：security-review 误报过滤工程（排除条款+判例+置信度阈值）、simplify 三代理并行审查清单——做评审类技能/多 Agent 评审编排时直接取（`external/claude-code-完备性扫尾.md` §5）。
